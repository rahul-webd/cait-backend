{
  "name": "cait-backend",
  "version": "1.0.0",
  "description": "backend for cait web app",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && (npm run deploy:app)",
    "deploy:app": "%INIT_CWD%/bin/deploy.sh",
    "start": "node ./dist/index.js",
    "test": "npm run build && npm run start"
  },
  "author": "rahul gautam",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "eosjs": "^22.1.0",
    "express": "^4.17.3"
  }
}
